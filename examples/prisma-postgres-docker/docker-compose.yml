services:
  postgres:
    image: postgres:16
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: prisma_docker
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
